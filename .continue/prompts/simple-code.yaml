name: Keep the code simple
version: 0.0.1
schema: v1
prompts:
  - name: Keep the code simple
    description: Avoid unnecessary comments.
    prompt: Keep the code simple. If you think a comment is necessary, try to simplify the code in order to be easily understandable without additional comments. Do not add any unnecessary comments.
